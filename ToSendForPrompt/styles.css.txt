body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: #f4f4f4;
}

header {
    background-color: #003366;
    color: white;
    padding: 10px 20px;
}

.banner-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.banner-top h1 {
    margin: 0;
    font-size: 2rem;
}

.banner-top nav {
    display: flex;
    gap: 16px;
}

.banner-top nav a {
    color: white;
    font-size: 0.9rem;
}

#status-bar {
    display: flex;
    gap: 20px;
    margin-top: 8px;
    font-size: 0.9rem;
}

.status-message {
    color: #8de18d;
}

main {
    display: grid;
    grid-template-columns: 320px 280px 380px 1fr;
    grid-template-rows: auto 1fr;
    grid-template-areas:
        "connection connection list editor"
        "stations links list editor";
    gap: 20px;
    padding: 12px;
}

#connection-section,
#station-section,
#stations-container,
#config-links-container,
#config-list-section {
    border: 1px solid #ccc;
    padding: 10px;
    border-radius: 4px;
    background: #fff;
}

#connection-section {
    grid-area: connection;
}

#stations-container {
    grid-area: stations;
}

#config-links-container {
    grid-area: links;
}

#config-list-section {
    grid-area: list;
    display: flex;
    flex-direction: column;
    min-height: 70vh;
}

#station-section {
    grid-area: editor;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    min-height: 70vh;
}

#station-details {
    flex-grow: 1;
    overflow-y: auto;
    max-height: 48vh;
}

#systemModeActions {
    margin-top: 10px;
}

.form-row {
    display: flex;
    gap: 6px;
    align-items: center;
    flex-wrap: wrap;
}

#serverInput {
    min-width: 220px;
}

#log-window {
    display: none;
}

.station-item {
    padding: 4px 8px;
    border-bottom: 1px solid #ccc;
    cursor: pointer;
}

.station-item:hover {
    background-color: #eef;
}

.saved-server-item {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 6px;
}

.saved-server-item a {
    min-width: 140px;
}

.saved-server-item button {
    padding: 2px 6px;
    cursor: pointer;
}

.hardening-panel {
    margin-top: 14px;
    border-top: 1px solid #ddd;
    padding-top: 10px;
}

.hardening-check-list {
    margin-top: 8px;
    display: flex;
    flex-direction: column;
    gap: 6px;
}

.hardening-check-item {
    border: 1px solid #ddd;
    background: #fafafa;
    padding: 6px;
    border-radius: 4px;
}

.delete-btn {
    color: #b00020;
    border-color: #b00020;
}

.station-links {
    margin-top: 0;
    border-top: 0;
    padding-top: 0;
}

.station-links a {
    display: block;
    margin-bottom: 6px;
}

.panel-title-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 8px;
}

#configItemList {
    flex: 1;
    overflow-y: auto;
}

.config-list-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 8px;
    border: 1px solid #ddd;
    border-radius: 4px;
    padding: 6px;
    margin-bottom: 6px;
}

.config-list-actions {
    display: flex;
    gap: 6px;
}

.category-panel {
    margin-top: 12px;
    border-top: 1px solid #ddd;
    padding-top: 10px;
}

.category-rows {
    display: flex;
    flex-direction: column;
    gap: 6px;
    max-height: 260px;
    overflow-y: auto;
    margin-bottom: 8px;
}

.category-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 10px;
    padding: 4px 6px;
}

.category-rows .category-row:nth-child(odd) {
    background-color: #ffffff;
}

.category-rows .category-row:nth-child(even) {
    background-color: #f2f2f2;
}

.category-name {
    font-family: Consolas, monospace;
    min-width: 80px;
}

.breaknote-panel {
    margin-top: 12px;
    border-top: 1px solid #ddd;
    padding-top: 10px;
}

.breaknote-columns {
    display: none;
}

.breaknote-link {
    display: block;
    margin-bottom: 4px;
}

.breaknote-grid {
    display: grid;
    grid-template-columns: 70px 90px 1fr 1fr 80px;
    gap: 6px;
    align-items: center;
    margin-top: 8px;
    border: 1px solid #ddd;
    padding: 8px;
}

.breaknote-header {
    text-align: center;
    font-weight: bold;
    font-size: 0.85rem;
}

.breaknote-rows {
    grid-column: 1 / -1;
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-top: 8px;
}

.breaknote-row {
    display: grid;
    grid-template-columns: 70px 90px 1fr 1fr 80px;
    gap: 6px;
    align-items: center;
}

.breaknote-row input {
    min-width: 0;
}

.remove-line-btn {
    cursor: pointer;
}

.list-subheader {
    margin-top: 10px;
    margin-bottom: 6px;
    font-weight: bold;
    color: #222;
}

.workflow-item {
    padding: 4px 2px;
    color: #111;
}

.category-list-item {
    grid-template-columns: 1fr 1fr auto;
}

.category-usage {
    text-align: center;
    color: #333;
}

.template-panel {
    margin-top: 12px;
    border-top: 1px solid #ddd;
    padding-top: 10px;
}

.template-columns {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
}

.template-columns > div:first-child {
    display: none;
}

.template-link {
    display: block;
    margin-bottom: 4px;
}

.template-rows {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-top: 10px;
    margin-bottom: 10px;
}

.template-row {
    display: grid;
    grid-template-columns: 80px 120px 140px 120px 1fr 50px;
    gap: 6px;
    align-items: center;
}

.template-row input,
.template-row select {
    min-width: 0;
}

.remove-template-row-btn {
    cursor: pointer;
}

.format-panel {
    margin-top: 12px;
    border-top: 1px solid #ddd;
    padding-top: 10px;
}

.format-columns {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
}

.format-columns > div:first-child {
    display: none;
}

.format-link {
    display: block;
    margin-bottom: 4px;
}

.format-rows {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-top: 10px;
    margin-bottom: 10px;
    max-height: 360px;
    overflow-y: auto;
}

.format-row {
    display: grid;
    grid-template-columns: 40px 1fr;
    gap: 8px;
    align-items: center;
}

.format-hour {
    font-family: Consolas, monospace;
    font-weight: bold;
}

.generation-panel {
    margin-top: 12px;
    border-top: 1px solid #ddd;
    padding-top: 10px;
}

.generation-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
}

.generation-grid > div .form-row + .form-row {
    margin-top: 0.5rem;
}

.calendar-block {
    margin-top: 10px;
    border: 1px solid #ddd;
    padding: 8px;
}

.calendar-nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 6px;
}

.calendar-weekdays {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 4px;
    font-size: 0.8rem;
    margin-bottom: 4px;
}

.calendar-grid {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 4px;
}

.calendar-cell {
    padding: 8px;
    text-align: center;
    border: 1px solid #ccc;
    cursor: pointer;
    user-select: none;
    background-color: #f9f9f9;
}

.calendar-cell:hover {
    background-color: #eef;
}

.calendar-cell.selected {
    background-color: #cce;
    border-color: #99c;
}

.calendar-cell.disabled {
    background-color: #eee;
    border-color: #ddd;
    cursor: default;
}

.schedule-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
    margin-top: 8px;
    margin-bottom: 8px;
}

.schedule-link {
    display: block;
    margin-bottom: 4px;
}

#schedList {
    display: none;
}

.editor-action-bar {
    margin-top: auto;
    border-top: 1px solid #ddd;
    padding-top: 10px;
    display: flex;
    gap: 8px;
}

#saveCategoriesBtn,
#newBreaknoteBtn,
#saveBreaknoteBtn,
#deleteBreaknoteBtn,
#newTemplateBtn,
#saveTemplateBtn,
#deleteTemplateBtn,
#newFormatBtn,
#saveFormatBtn,
#deleteFormatBtn,
#genSaveSettingsBtn,
#schedSaveBtn,
#schedDeleteBtn {
    display: none;
}

.system-settings-panel {
    position: fixed;
    top: 72px;
    right: 12px;
    width: min(720px, calc(100vw - 24px));
    max-height: calc(100vh - 84px);
    overflow-y: auto;
    border: 1px solid #ccc;
    border-radius: 4px;
    background: #fff;
    padding: 12px;
    z-index: 100;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
}

.system-settings-panel.inline {
    position: static;
    top: auto;
    right: auto;
    width: 100%;
    max-height: none;
    overflow-y: visible;
    border: 0;
    border-radius: 0;
    padding: 0;
    z-index: auto;
    box-shadow: none;
}

.system-defaults-panel {
    border-bottom: 1px solid #ddd;
    padding-bottom: 10px;
    margin-bottom: 10px;
}

.logging-panel {
    border-bottom: 1px solid #ddd;
    padding-bottom: 10px;
    margin-bottom: 10px;
}

.user-config-panel {
    border-bottom: 1px solid #ddd;
    padding-bottom: 10px;
    margin-bottom: 10px;
    transition: all 0.2s ease;
}

.user-config-panel.enabled {
    width: 100%;
}

.users-list {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-top: 6px;
}

.user-row {
    display: grid;
    grid-template-columns: 1fr 1.2fr 140px 80px;
    gap: 8px;
    align-items: center;
}

.user-password-wrap {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 6px;
    align-items: center;
}

.user-eye-btn {
    min-width: 36px;
}

.system-defaults-panel .form-row label {
    min-width: 180px;
}

.system-defaults-panel .form-row input {
    min-width: 220px;
}

.server-connection-item + .server-connection-item {
    margin-top: 0.5rem;
}

.logging-panel .form-row label,
.user-config-panel .form-row label {
    min-width: 180px;
}

.hidden {
    display: none;
}

.empty-state {
    color: #555;
}
